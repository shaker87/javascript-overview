var name = 'shaker';

if(name.length > 5){
    name = 'saker';
}
console.log(name);

const country = 'bangladesh'; //const use when don't change value
console.log(country);

let age = 16; // let use when i changed value

age = 30;

console.log(age);